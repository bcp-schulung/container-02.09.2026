apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-traefik-tls
  namespace: traefik
spec:
  secretName: wildcard-traefik-tls
  issuerRef:
    name: letsencrypt-staging
    kind: ClusterIssuer
  dnsNames:
    - "*.traefik.container.it-scholar.com"
